# requirements-v2.txt
# Jarvis v2 dependencies — install with:
#   pip install -r requirements-v2.txt

# v1 deps (kept here for completeness)
requests>=2.31.0
numpy>=1.26.0
watchdog>=3.0.0
aiohttp>=3.9.0
rank_bm25>=0.2.2
filelock>=3.13.0

# v2 new deps
jinja2>=3.1.2          # ERS prompt templates (lib/ers/augmentor.py)
psutil>=5.9.8          # ERS RAM gate (lib/ers/augmentor.py)
pydantic>=2.5.0        # ERS schema validation (lib/ers/schema.py)
httpx>=0.27.0          # Model adapters async HTTP (lib/models/adapters/)
cryptography>=42.0.0   # AES-256 secrets store (lib/security/secrets.py)
pygls>=1.3.0           # LSP server (services/jarvis_lsp.py)
lsprotocol>=2023.0.1   # LSP types (services/jarvis_lsp.py)
fastapi>=0.110.0       # HTTP sidecar (services/jarvis_lsp.py)
uvicorn>=0.29.0        # ASGI server (services/jarvis_lsp.py)
nest_asyncio>=1.6.0    # Sync/async bridge (lib/models/router.py)
pytest>=8.0.0          # Test runner
pytest-asyncio>=0.23.0 # Async test support
pyyaml>=6.0.1          # YAML chain loading (lib/ers/chain.py — FIX-ERS-1)
